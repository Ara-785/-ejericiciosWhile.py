# Crear una lista de números  
lista_numeros = [1, 2, 3, 4, 5]  

# Usar map con una función lambda para elevar cada número al cuadrado  
numeros_al_cuadrado = list(map(lambda x: x ** 2, lista_numeros))  

# Imprimir la lista de números al cuadrado  
print("Números al cuadrado:")  
print(numeros_al_cuadrado)
